import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DI04_TareaEvaluativa_25',
  webDir: 'www'
};

export default config;
